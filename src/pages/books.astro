---
import PlaceholderPage from '../components/PlaceholderPage.astro';
import { getSections } from '../data/catalogue';
import { getTranslation } from '../data/i18n';

const title = getTranslation('en', 'pages.books.title');
const description = getTranslation('en', 'pages.books.description');
---

<PlaceholderPage
  locale="en"
  title={title}
  description={description}
  sections={getSections('works', 'en')}
/>
